
REV A : Learnt makefiles and linker script and tried it with arm toolchain
        NOTE: Download arm tool chain with pkg format from ARM website and add the bin folder path to PATH variable

REV B : Learnt how to build static libraries and compile them with main.
        Did some re-org to the folder structure. Makefile is in _app folder
